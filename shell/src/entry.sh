# ==============================================================================
# L5  入口
# ==============================================================================

main() {
    sys::require_root
    sys::require_debian
    pkg::ensure_deps || { log::err "依赖准备失败，无法继续。"; exit 1; }
    self::install_shortcut "$@"
    menu::main "$@"
}

main "$@"
